import static org.junit.Assert.*;
import org.junit.Test;

public class CalculadoraMainTest {
    
    @Test
    public void testMain() {
        // Probando el método main de la clase CalculadoraMain
        CalculadoraMain.main(new String[]{}); // Ejecutar el método main
        // No hay nada que verificar aquí ya que solo inicia la interfaz gráfica
    }
}